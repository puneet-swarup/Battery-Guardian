<Window x:Class="BatteryGuardian.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="Assets/app.ico"
        Title="Battery Guardian Settings"
        Height="Auto"
        Width="400"
        SizeToContent="Height"
        MinHeight="240"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="High Threshold (%):"
                   VerticalAlignment="Center"
                   Foreground="#333333"
                   Margin="0,0,10,14"/>
        <TextBox x:Name="HighThresholdBox"
                 Grid.Row="0" Grid.Column="1"
                 Margin="0,0,0,14"
                 Padding="4"/>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Low Threshold (%):"
                   VerticalAlignment="Center"
                   Foreground="#333333"
                   Margin="0,0,10,14"/>
        <TextBox x:Name="LowThresholdBox"
                 Grid.Row="1" Grid.Column="1"
                 Margin="0,0,0,14"
                 Padding="4"/>

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Repeat Alert (sec):"
                   VerticalAlignment="Center"
                   Foreground="#333333"
                   Margin="0,0,10,14"/>
        <TextBox x:Name="RepeatIntervalBox"
                 Grid.Row="2" Grid.Column="1"
                 Margin="0,0,0,14"
                 Padding="4"/>

        <TextBlock Grid.Row="3" Grid.Column="0"
                   Text="Updates:"
                   VerticalAlignment="Center"
                   Foreground="#333333"
                   Margin="0,0,10,20"/>
        <CheckBox x:Name="CheckForUpdatesCheckBox"
                  Grid.Row="3" Grid.Column="1"
                  VerticalAlignment="Center"
                  Content="Automatically check on startup"
                  Margin="0,0,0,20"/>

        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                Content="Save Settings"
                Width="120" Height="32"
                HorizontalAlignment="Center"
                Click="SaveButton_Click"/>
    </Grid>
</Window>