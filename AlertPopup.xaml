<Window x:Class="BatteryGuardian.AlertPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alert" Height="110" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border Background="#FFC107" CornerRadius="8" Margin="10">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Battery Guardian" FontWeight="Bold" Foreground="#333"/>
            <TextBlock x:Name="MessageText" Grid.Row="1" TextWrapping="Wrap" Foreground="#333" Margin="0,5,0,0"/>
        </Grid>
    </Border>
</Window>